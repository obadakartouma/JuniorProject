شرح تنفيذ UC-05
التدفق الرئيسي
الدخول إلى صفحة المسارات: GET /api/courses/

اختيار مسار للتعديل: GET /api/courses/{pathid}/

عرض نموذج التعديل: Frontend يعرض البيانات الحالية

إرسال البيانات المعدلة: PUT /api/courses/{pathid}/update/

التحقق من الصحة: Backend يتحقق من البيانات

تحديث البيانات: حفظ التغييرات في قاعدة البيانات

إظهار رسالة نجاح: إرجاع استجابة بنجاح

التدفق البديل A1 - بيانات غير مكتملة
النظام يكشف الحقول الفارغة

يظهر رسالة خطأ لكل حقل

يحتفظ بالبيانات المدخلة للتصحيح

التدفق البديل A2 - بيانات غير صالحة
النظام يكشف الأخطاء في صيغة البيانات

يظهر رسالة خطأ توضيحية

يحتفظ بالبيانات الصحيحة